# LAN IP of the machine running the backend (not localhost — device/emulator needs
# to reach it over the network). Find yours with `ipconfig` (Windows) or `ifconfig` (mac/linux).
EXPO_PUBLIC_API_URL=http://192.168.1.75:8000
